"use client";

// TODO (Phase 2): replace with Recharts bar chart showing delay-cause
// breakdown (Weather, Carrier, NAS, Late Aircraft) per carrier.
// See PRD Req 4.3.

export default function DelayChart() {
  return (
    <div className="rounded-xl bg-slate-900 border border-slate-800 h-72 flex items-center justify-center text-slate-500">
      Delay distribution chart placeholder — Recharts bar chart goes here
    </div>
  );
}
